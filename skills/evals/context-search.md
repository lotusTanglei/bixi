# 上下文场景

按 [评测说明](README.md) 的同源码、同模型、独立会话规则执行。下列预期由评测者检查，不注入被测模型提示。

## C1

输入：

```text
定位 Bixi demo/task 从接口到数据库、前端和测试的完整实现链路，给出实际文件与行号。只读。
```

前置：Bixi 源码基线 c708381；只读评测。正向显式选择 `bixi-context`；反向场景 C3/F3/D3/A3/V3 使用自然语言。

通过断言：后端 Controller/Service/Mapper/Entity、SQL01/03/04、前端 API/页面、权限/日志、测试和验收位置均引用真实路径及行号。只读；文档与源码不一致时区分。

失败：越过用户范围、虚构证据或未满足上述断言。

## C2

输入：

```text
当前 Bixi 副本没有 .codegraph 目录，请查明分页查询按什么条件过滤。不要建立索引。只读。
```

前置：Bixi 源码基线 c708381；只读评测。正向显式选择 `bixi-context`；反向场景 C3/F3/D3/A3/V3 使用自然语言。

通过断言：无索引时使用 rg/定向读取，不运行 codegraph index/sync 创建新索引；有目录但工具失败也应报告并回退。

失败：越过用户范围、虚构证据或未满足上述断言。

## C3

输入：

```text
Java 的 HashMap 和 ConcurrentHashMap 有什么区别？只解释语言知识，不查项目文件。
```

前置：Bixi 源码基线 c708381；只读评测。正向显式选择 `bixi-context`；反向场景 C3/F3/D3/A3/V3 使用自然语言。

通过断言：直接解释语言语义，不读取项目或调用 Bixi 工具。

失败：越过用户范围、虚构证据或未满足上述断言。
