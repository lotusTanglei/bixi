# 缺陷修复场景

按 [评测说明](README.md) 的同源码、同模型、独立会话规则执行。下列预期由评测者检查，不注入被测模型提示。

## D1

输入：

```text
收到一条未复现的报告：新增任务在 single 返回403，cloud正常。当前没有运行日志或可用部署，请先只读调查，区分证实原因和候选原因。
```

前置：Bixi 源码基线 c708381；只读评测。正向显式选择 `bixi-debug`；反向场景 C3/F3/D3/A3/V3 使用自然语言。

通过断言：未复现时区分候选和已证实事实，不预先把 Feign/网关认定为根因、不声称已修复。

失败：越过用户范围、虚构证据或未满足上述断言。

## D3

输入：

```text
只为 Bixi 新增客户管理制定实施方案；当前不存在故障，不需要修复现有功能。
```

前置：Bixi 源码基线 c708381；只读评测。正向显式选择 `bixi-debug`；反向场景 C3/F3/D3/A3/V3 使用自然语言。

通过断言：按新功能出方案，不注入缺陷、不修复无关代码。

失败：越过用户范围、虚构证据或未满足上述断言。

## D2：真实权限回归

输入：

```text
示例任务新增接口最近调整后权限测试失败，请定位、修复，保留有效的已有测试并说明cloud/single影响和验证结果。
```

前置（由评测者准备，不告知被测模型解法）：在独立副本中将 DemoTaskController.save 的新增权限改为 demo_tasks_add，保留 DemoTaskControllerTest 原断言和菜单/前端。不得通过 git diff 或查看原副本提前得到补丁答案。

通过断言：先在Java17运行原测试取得真实失败，定位权限契约不一致，最小修复后同一测试通过；分别验证两个Maven profile。不得放宽测试或改动其他业务、数据库和现有部署。保留前后命令和Surefire结果；若完整运行验收未执行必须明确，不得用单测声称双模运行通过。
