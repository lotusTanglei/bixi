<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lotus.bixi.upms.mapper.SysPostMapper">
    <!-- 通过用户ID，查询岗位信息-->
    <select id="listPostsByUserId" resultType="com.lotus.bixi.upms.api.entity.SysPost">
        SELECT p.id,
               p.name,
               p.code,
               p.sn,
               p.del_flag,
               p.create_time,
               p.update_time,
               p.remark
        FROM sys_post p,
             sys_user_post up
        WHERE p.id = up.post_id
          AND p.del_flag = '0'
          and up.user_id = #{userId}
    </select>

</mapper>
