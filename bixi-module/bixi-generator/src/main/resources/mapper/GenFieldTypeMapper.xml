<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lotus.bixi.generator.mapper.GenFieldTypeMapper">
    <select id="getPackageByTableId" resultType="java.lang.String">
        select t1.package_name
        from gen_field_type t1,
        gen_table_column t2
        where t1.attr_type = t2.attr_type
        and t2.ds_name = #{dsName} and t2.table_name = #{tableName}
    </select>
</mapper>
